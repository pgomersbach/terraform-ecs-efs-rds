# ok add plugin rex-ray
# ok verwijder efs
# aparte module voor taskdef /task / service
# verplaats lb naar appl
# awsvpc verwijderen
# voeg acl toe aan lb ipv default 
# bedachte naam alleen - en geen _
# data source voor subnets
# data source voor vpc id
# subnets als array
# storage ebs / efs aan ecs-appl module
# taak met params. image, port, taak naam, mem, cpu
ebs volume opties, size, type iops
name resources from env/name
toevoegen tags
output zinvolle info
modules herbruikbaar maken
verwijder mount target als destroy optie
r53 aan service toevoegen
add remote state
gebruik workspaces
install elk stack
change README
test loadbalancer vanuit mn
