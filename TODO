# ok add plugin rex-ray
# ok verwijder efs
# aparte module voor taskdef /task / service
# verplaats lb naar appl
# awsvpc verwijderen
# voeg acl toe aan lb ipv default 
# bedachte naam alleen - en geen _
# data source voor subnets
# data source voor vpc id
# subnets als array
# storage ebs / efs aan ecs-appl module
# taak met params. image, port, taak naam, mem, cpu
# ebs volume opties, size, type iops
# test loadbalancer vanuit mn
# health check toevoegen aan taak
# rds toevoegen
# r53 aan lb toevoegen
# load balancer kiezen
# kiezen tussen standaard taak of eigen taak definitie
# kibana toevoegen
# beats toevoegen
# modules herbruikbaar maken
# output zinvolle info na deployment
# install elk stack in twee delen, deamon en replica
# voeg apm-server toe
# apm-server health check
# apm-server in heartbeat
# install elk stack in twee delen, deamon en replica
# add task number (bij replica)
# auto scaling instances
schematische weergave elk stack
kibana custom dashboards
es gebruik datavol nakijken
scheduled task toevoegen voor opruimen indices
redundant es cluster (wacht op name resolving of bedenk een list)
ssl keys toevoegen aan loadbalancer
auth toevoegen aan es cluster
tag resources from env/name
toevoegen tags
verwijder mount target als destroy optie
add remote state
gebruik workspaces
change README
documentatie update elk
documentatie gebruik tf ecs
